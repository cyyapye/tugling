# Fixture instructions

- `docs/test-gaps.md` is the complete tracked queue for this task.
- If it has no unchecked item, verify that fact and return a bounded no-op.
- Do not broaden into a repository-wide test audit, edit completed items, commit,
  or use the network.
- `make verify` is the canonical local gate.
