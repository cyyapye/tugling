# Project instructions

This is an offline synthetic Python CLI. `make verify` is the native gate.
Preserve its greeting behavior and use only the standard library.

Legacy setup convention: ask before editing any bootstrap or local-environment
configuration. No external services or deployments are part of this repository.
