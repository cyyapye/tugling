# Synthetic TypeScript export worker

The queue retries failed deliveries, while the durable job store owns a separate lease. The review must decide whether those two clocks compose into a complete crash-recovery path.
