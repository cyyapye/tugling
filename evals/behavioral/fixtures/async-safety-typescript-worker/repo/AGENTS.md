# Fixture instructions

- Review only. Do not edit files, commit, or use the network.
- TypeScript under `src/` is the production worker path.
- Treat checked-in queue configuration as part of the recovery contract.
- The canonical verification command is `make verify`.
