# Fixture instructions

- This is a plan-only task. Do not edit files, commit, or use the network.
- Preserve the public CLI behavior while callers migrate.
- Ground the plan in `docs/api-contract.md`, current callers, tests, and the
  repository's verification command.
- Separate local proof from rollout and deployed proof.
