# Account lookup fixture

The library still exposes username-based lookup. The next API uses stable
account IDs, but the CLI and one documented external integration still depend
on the legacy call.

Run `make verify` for the current contract tests.
