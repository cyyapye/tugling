# Project contributor instructions

- Keep this public example repository synthetic-only.
- Existing project instructions remain authoritative when agent tooling is added.
- `make verify` is the canonical local gate.
- Do not change product code while assessing an agent-tool setup.
- Pull requests require the checks in `.github/workflows/ci.yml`.
